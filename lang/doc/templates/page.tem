<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="theme-color" content="#375EAB">
	<title>{{ Titlecase .Slot.Name }} - Tapestry Documentation </title>
	<link type="text/css" rel="stylesheet" href="style.css">
	<link type="text/css" rel="stylesheet" href="custom.css">
</head>
<body>
	{{/* -------------------------------------------------
	navigation 
	-------------------------------------------------	*/}}
	<div id="topbar" class="wide"><div class="container">
		<div class="top-heading" > Tapestry Documentation </div>
	</div></div>

	{{/* -------------------------------------------------
	page container
	-------------------------------------------------	*/}}
	<div id="page" class="wide" style="outline: 0px;">
		<div class="container">

			<h1>{{ Titlecase .Slot.Name }}</h1>

			<h3 id="pkg-commands" class="Doc-commandsHeader">
				Commands:
			</h3>
			<section class="Doc-commnds">
				{{ range $k, $v := .Part }}
				<div class="Doc-partition">
					<h2 id="Fmt" data-kind="partition" class="Doc-partitionHeader">
						{{Pascal $k}}
					</h2>
					{{ range $i, $cmd := $v.Types }}
					{{ if $i }}<hr>{{end }}
					{{/* Signature */}}
					<span class="Doc-commandComment">
						{{ Lines $cmd.Markup.comment }}
					</span>
					<div class="Doc-commandSignature">
						<pre><a class="Doc-source" href="{{ .SourceLink }}">
						{{- Pascal $cmd.Lede }}
						{{- range $i, $t := $cmd.Terms }}
						{{- if $t.Optional }}[{{end}}
						{{- if $t.Label}}
						{{- if not $i}} {{end}} 
						{{- $t.Label }}
						{{- end }}:
						{{- if $t.Optional }}]{{end}}
						{{- end }}{{/* public terms  */ -}}
						</a></pre>
					</div>

					<div class="Doc-commandBody">
						Terms:
						<ul class="Doc-commandArgs">
						{{- range $i, $t := $cmd.Terms }}
							<li>
								<span class="Doc-argLabel">
								{{- if $t.Label}}{{ $t.Label }}{{ else }}
								{{ Pascal $cmd.Lede }}
								{{- end -}}
								</span><span class="Doc-argSep">:</span>
								<span class="Doc-argType">
								<a class="Doc-type" href="{{ TypeLink  $t.Type }}">{{ Pascal $t.Type.TypeName}}</a>
								</span>
							</li>
						{{- end }}{{/* public terms  */}}
						</ul>
						{{/* Slot UL */}}
						{{ if $cmd.Slots }}
						Slots:
						<ul class="Doc-commandSlots">
							{{ range $cmd.Slots }}
							<li><a class="Doc-type" href="{{ TypeLink  . }}">{{ Pascal .Name}}</a>
								</span></li>
							{{ end }}{{/* range slots */}}
						</ul>{{ end }}{{/* if slots */}}
					</div>

					{{end }}{{ end }}{{/* $i, $cmd */}}
				</div>
				{{/* .Part */}}
			</section>

			{{/*-------------------------------------------------
			footer
			------------------------------------------------- */}}
			<div id="footer">
				<b>Tapestry - a story engine for games.</b><br>
				The content of this page is licensed under the Creative Commons Attribution 3.0 License, and code is licensed under a BSD license.<br>
				Portions of this page are modifications based on work created and <a href="/readme/policies">shared by Google</a> and used according to terms described in the <a href="https://creativecommons.org/licenses/by/4.0/">Creative Commons 4.0 Attribution License</a>.<br>

			</div>

			{{/*-------------------------------------------------
			end
			------------------------------------------------- */}}
		</div>{{/*container*/}}
	</div>{{/*page*/}}
</body>
</html>