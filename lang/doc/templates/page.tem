<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="theme-color" content="#375EAB">
	<title>{{ Title .Name }} - Tapestry Reference </title>
	<link type="text/css" rel="stylesheet" href="/api/style.css">
	<link type="text/css" rel="stylesheet" href="/api/custom.css">
</head>
<body>
	{{ define "command-title" }}
		{{- Title .Name }}
	{{- end }}
	{{ define "command-title-alt" }}
		{{- Pascal .Lede }}
		{{- range $i, $t := .Terms }}
			{{- if not $i }}
			{{- if $t.Label }} 
			{{ Camel $t.Label }}
			{{- end }} 
			{{- end }}
		{{- end }}
	{{- end }}
	{{/* -------------------------------------------------
	navigation 
	-------------------------------------------------	*/}}
	<div id="topbar" class="wide">
		<div class="container">
			<div class="top-heading"><a href="{{ApiHome}}"><img class="gdoc-brand__img" src="/brand.svg" alt="">Tapestry Reference</a></div>
		</div>
	</div>

	{{/* -------------------------------------------------
	page container
	( the more correct way to do this would be to have each
	  type render itself using "blocks" to structure the page "chrome"
	-------------------------------------------------	*/}}
	<div id="page" class="wide" style="outline: 0px;">
		<div class="container">
			{{ if .Slot }}
				{{/* the slot, and all its comands */}}
				{{ template "slot.tem" . }}
			{{ else if .Types }}
				{{/* the idl, and all its types */}}
				{{ template "idl.tem" . }}
			{{ else if .Commands }}
				{{/* index of all commands */}}
				{{ template "alpha.tem" . }}
			{{ else }}
				{{/* root index page */}}
				{{ template "api.tem" . }}
			{{ end }}
			{{/*----------------------------------
			page footer ( inside the container )
			------------------------------------------------- */}}
			<div id="footer">
				<b>Tapestry - a story engine for games.</b><br>
				The content of this page is licensed under the Creative Commons Attribution 4.0 License, and code is licensed under a BSD license.<br>
        Portions of this page are modifications based on work created and <a href="https://developers.google.com/terms/site-policies">shared by Google</a> and used according to terms described in the <a href="https://creativecommons.org/licenses/by/4.0/">Creative Commons 4.0 Attribution License</a>.<br>
			</div>
		</div>{{/*container*/}}
	</div>{{/*page*/}}
</body>
</html>