// {{.Name}}, a type of str enum.
const Z_{{Pascal .Name}}_Name = "{{.Name}}"

const (
{{- range $i, $opt := .Options }}
  W_{{Pascal $.Name}}_{{Pascal $opt}} = "{{StringKey $opt}}"
{{- end }}
)

var Z_{{Pascal .Name}}_T = typeinfo.Str{
	Name: Z_{{Pascal .Name}}_Name,
	Options: []string{
	{{- range $i, $opt := .Options }}
		W_{{Pascal $.Name}}_{{Pascal $opt}},
	{{- end }}
	},
	{{- template "markup.tmpl" .}}
}
